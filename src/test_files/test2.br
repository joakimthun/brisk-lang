pkg main

ext fn puts(u8 *str) -> i32

fn myfn(i32 arg1, i32 arg2, i32 arg3, i32 arg4) -> i32 {
	let x = 10 + 2
	puts("Hello myfn!")
	ret arg2
}

fn main() -> i32 {
	let str = "Hello Brisk!"
	puts(str)

	u8 *x = "str"

	myfn(1, 2, 3, 4)
	ret 0
}